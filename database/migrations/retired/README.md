# Retired migrations

These migration files were replaced on 2026-06-12 by a clean set in `database/migrations/`.

They are kept for reference only and are **not** run by Laravel.

For a fresh local database, use:

```bash
php artisan migrate:fresh --seed
```
